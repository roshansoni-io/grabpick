# GrabPick environment configuration
# Copy to .env and adjust values for your setup.

# PostgreSQL (pgvector) connection URL
DATABASE_URL=postgresql+psycopg://localhost:5432/grabpick

# ONNX model paths (relative to the project root)
GRABPICK_DETECTOR_MODEL=model/detector/scrfd_500m_gnkps.onnx

GRABPICK_EMBEDDING_MODEL=model/embedding/edgeface_xs_gamma_06.onnx

# Face recognition match threshold (cosine similarity, 0.0-1.0)
GRABPICK_THRESHOLD=0.45

# Vector distance metric for nearest-neighbour search.
# One of: cosine (default), l2, inner_product
GRABPICK_DISTANCE_METRIC=cosine

# Per-IP rate limiting (requests per window, window in seconds)
GRABPICK_RATE_LIMIT_MAX=60
GRABPICK_RATE_LIMIT_WINDOW=60

# Set to 1 only when behind a reverse proxy; enables trusting X-Forwarded-For.
# Leave unset otherwise, since that header is client-spoofable.
GRABPICK_TRUST_PROXY=